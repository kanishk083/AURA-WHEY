{
  "name": "aura-whey-storefront",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node scripts/dev-server.mjs",
    "test": "node --test tests/*.test.cjs",
    "check": "node --check app.js && node --check product-viewer.js && node --check shopify.js && npm test",
    "build": "node scripts/build.mjs"
  },
  "dependencies": {
    "sharp": "^0.35.4"
  }
}
