{
  "name": "aura-whey-storefront",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.cjs",
    "check": "node --check app.js && node --check shopify.js && npm test",
    "build": "node scripts/build.mjs"
  }
}
